import React from 'react'

const Title = ({title}) => {
  return (
    <div className='font-manrope text-heading font-heading my-[15px] '>
        <span className='flex justify-center'>{title}</span>
    </div>
  )
}

export default Title